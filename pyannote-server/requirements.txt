fastapi>=0.141.1
uvicorn>=0.27
python-multipart>=0.0.32
pyannote.audio>=4.0.7
# torch įdiegiamas atskirai Dockerfile'e (CPU ar CUDA variantas priklauso nuo bazinio image)

# Testavimui (pytest test_server.py, test_diarize_integration.py):
pytest>=9.1.1

# hf_transfer: paralelinis, atsparesnis modelių atsisiuntimas iš Hugging Face.
# RunPod'e PRAKTIŠKAI BŪTINAS - be jo dideli modeliai dažnai timeout'ina/nutrūksta
# atsisiunčiant, ir serveris nepasileidžia. Įjungiama: HF_HUB_ENABLE_HF_TRANSFER=1.
# Rankiniam diegimui BŪTINAI į venv: pyannote-server/.venv/bin/python -m pip install hf_transfer
hf_transfer>=0.1.9

# ─────────────────────────────────────────────────────────────────────────
# ⚠️  ŠIS failas naudoja MINIMALIAS (>=) versijas - patogu vystymui, bet RIZIKINGA
# produkcijai/RunPod (po kelių mėnesių gali įsidiegti nesuderinamos naujesnės
# versijos). DETERMINISTINIAM diegimui naudokite užfiksuotus lock failus:
#   requirements-cpu.lock.txt  - CPU (+ torch cpu, žr. failo komentarus)
#   requirements-gpu.lock.txt  - GPU (+ torch cu124, žr. failo komentarus)
# Docker image'ai (Dockerfile / Dockerfile.gpu) jau naudoja lock failus.
# ─────────────────────────────────────────────────────────────────────────
