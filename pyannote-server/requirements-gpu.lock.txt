# Pyannote serveris - GPU (CUDA) priklausomybės, UŽFIKSUOTOS VERSIJOS.
#
# Naudojimas:
#   pip install torch==2.5.1 torchaudio==2.5.1 --index-url https://download.pytorch.org/whl/cu124
#   pip install -r requirements-gpu.lock.txt
#
# STATUSAS - SVARBU (sąžiningai):
# NEBUVO realiai išbandyta šioje aplinkoje (nėra GPU nei gated pyannote modelio
# prieigos). REKOMENDUOJAMAS suderinamas rinkinys pagal pyannote.audio 3.x + CUDA
# 12.4 dokumentaciją. Pirmo GPU diegimo metu BŪTINA patikrinti:
#   python -c "import torch; print(torch.cuda.is_available())"   # turi būti True
#   python -c "import pyannote.audio; print('OK')"
# ir realiu audio per /diarize. torch cu124 turi atitikti host CUDA - jei ne,
# keiskite į savo cuXXX ir atitinkamai atnaujinkite šias versijas.

pyannote.audio==4.0.7
fastapi==0.141.1
uvicorn==0.52.3
python-multipart==0.0.32
huggingface-hub==1.27.0
# numpy 2.5+ reikalauja Python >=3.12, o šie image'ai naudoja python3.11.
# 2.4.6 yra PASKUTINĖ versija su Requires-Python >=3.11.
#
# Kėlimas į 3.12 kol kas nepadeda: `av` (audio dekodavimas) naujausioje 18.0.0
# turi TIK cp311 wheel'ą, tad tektų jį nuleisti iki 15.1.0 - trys majorinės
# versijos atgal pakete, kuris yra tiesiogiai audio kelyje. Grįžti prie 3.12
# verta, kai `av` išleis cp312 wheel'ą; tada tai bus grynas kėlimas be nuleidimų.
numpy==2.4.6

# Testavimui:
pytest==9.1.1

# PASTABA: torch==2.5.1 ir torchaudio==2.5.1 NĖRA šiame faile - juos įdiekite
# ATSKIRAI su --index-url .../cu124 (žr. komentarą viršuje), nes PyPI numatytoji
# torch versija yra CPU, o mums reikia CUDA build'o.
