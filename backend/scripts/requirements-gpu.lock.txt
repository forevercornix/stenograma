# Stenograma backend - GPU (device=cuda) transkripcijos priklausomybės, UŽFIKSUOTOS.
#
# Naudojimas:
#   pip install -r scripts/requirements-gpu.lock.txt
#
# STATUSAS - SVARBU:
# - faster-whisper/ctranslate2/numpy/tokenizers/av/onnxruntime versijos - REALIAI
#   PATIKRINTOS (vartotojo Windows sesija, 2026-07), bet CPU režimu.
# - CUDA bibliotekų (nvidia-cublas-cu12/nvidia-cudnn-cu12) TIKSLIOS versijos, kurios
#   įsidiegė toje sesijoje, žemiau. Tačiau `device=cuda` realaus veikimo su GPU
#   ŠIOJE APLINKOJE NEBUVO IŠBANDYTA (nėra GPU). Jei nauja GPU/CUDA versija
#   nesuderinama - koreguokite nvidia-* versijas pagal savo CUDA (12.x).

faster-whisper==1.2.1
ctranslate2==4.8.1
# numpy 2.5+ reikalauja Python >=3.12, o šie image'ai naudoja python3.11.
# 2.4.6 yra PASKUTINĖ versija su Requires-Python >=3.11.
#
# Kėlimas į 3.12 kol kas nepadeda: `av` (audio dekodavimas) naujausioje 18.0.0
# turi TIK cp311 wheel'ą, tad tektų jį nuleisti iki 15.1.0 - trys majorinės
# versijos atgal pakete, kuris yra tiesiogiai audio kelyje. Grįžti prie 3.12
# verta, kai `av` išleis cp312 wheel'ą; tada tai bus grynas kėlimas be nuleidimų.
numpy==2.4.6
huggingface-hub==1.25.1
tokenizers==0.23.1
av==18.0.0
onnxruntime==1.28.0
tqdm==4.70.0

# CUDA runtime bibliotekos (versijos iš tos pačios sesijos, kur pip jas įtraukė).
# ctranslate2 4.8.x reikalauja cuDNN 9.x.
nvidia-cublas-cu12==12.9.2.10
nvidia-cudnn-cu12==9.24.0.43
